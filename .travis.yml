language: python
env:
    - PIP_USE_MIRRORS=true
install:
    - sudo apt-get update -qq
    - sudo apt-get install -q python3.1-dev
    - sudo apt-get install -q python3.3-dev
    - sudo pip install -U virtualenv==1.9.1
    - sudo pip install --use-mirrors tox
script: tox
